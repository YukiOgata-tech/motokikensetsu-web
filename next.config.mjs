/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    domains: [
      "tekisei-research.com",
      "images.unsplash.com"
    ],
  },
};

export default nextConfig;
